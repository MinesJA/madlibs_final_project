
<%= form_tag '/completed_stories' do %>

  <% @hash.each do |key,value| %>
    <%= label_tag :"#{key}:" %><br>

    <%= text_field_tag :"words[#{key}]" %><br>
    <% end %>

    <%= hidden_field_tag(:template_id, @template.id) %>
    <%= submit_tag "Create Story" %>

<% end %>
